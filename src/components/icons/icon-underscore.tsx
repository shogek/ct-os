import { FaRegWindowMinimize } from 'react-icons/fa6'
import { IconProps } from './types'

export default function IconUnderscore(props: IconProps) {
   return <FaRegWindowMinimize className={props.className} />
}
